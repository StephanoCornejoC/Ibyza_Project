# Core
# Techo <6.1: DRF 3.15.2 importa django.utils.cache.cc_delim_re, que no existe en
# Django 6.1. Sin el techo, pip resuelve 6.1.1 y el contenedor no arranca.
Django>=6.0.3,<6.1
djangorestframework==3.15.2
django-cors-headers==4.4.0

# Imágenes
Pillow>=11.0.0

# Variables de entorno
python-dotenv==1.0.1

# Base de datos (SQLite en dev → PostgreSQL en prod con una sola línea)
dj-database-url==2.3.0
psycopg2-binary>=2.9.9

# Servidor producción
gunicorn>=23.0.0
whitenoise==6.7.0

# Filtros en API
django-filter==24.3

# Admin moderno e intuitivo para clientes
django-unfold==0.42.0

# Pasarela de pago Culqi (Perú)
culqipy==0.2.4
requests>=2.32.2

# Email transaccional (3000/mes gratis, alta deliverability)
resend>=2.0.0

# Storage en Cloudflare R2 (S3-compatible) para media files
django-storages[s3]>=1.14.4
boto3>=1.35.0
